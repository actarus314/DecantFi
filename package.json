{
  "name": "stellar-swap",
  "version": "0.1.0",
  "private": true,
  "description": "Recommandeur de route nette BLND -> USDC/EURC sur Stellar (meta-agregateur de cotations, read-only).",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "quote": "tsx cli/index.ts",
    "history": "tsx cli/history.ts",
    "export": "tsx cli/history.ts export",
    "tick:once": "tsx collector/tick-once.ts",
    "collector": "tsx collector/daemon.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0",
    "soroswap-router-sdk": "^1.4.6"
  }
}
